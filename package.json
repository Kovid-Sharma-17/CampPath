{
  "name": "camp-path",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "python3 -m http.server 5173 --bind 127.0.0.1 --directory dist",
    "refresh:data": "python3 scripts/build_camp_path.py --refresh"
  }
}
