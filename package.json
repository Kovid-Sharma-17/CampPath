{
  "name": "accesspath-pilot",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "python3 -m http.server 5173 --bind 127.0.0.1 --directory dist"
  }
}
